{% extends 'base.html.twig'%}

{% block title 'Gérer la boutique '%}

{% block body %}
    <body>
        <section id="fiche-produit">
            <div class="container">
                <div class="row">
                    <div class="col-lg-12">
                        <div class="liste-title">
                            <h2>Gestion de la boutique </h2>
                        </div>

                        <table class="table table-striped">
           
                            <tbody>
                            <tr>
                                {% for prop in produits %}
                                    <td> {{prop.nomProduit }} </td>
                                    <td>
                                        <a href="{{ path('admin.property.edit', {id: prop.idProduit}) }}" class="btn btn-secondary"> Editer </a> 
                                    </td>        
                            </tr>
                                {% endfor %}
                            </tbody>

                        </table>
                    </div>
                </div>
            </div>            
        </section>

            {# <div class = "container">

                <h2> Les produits disponibles </h2>

                <div class="row flex">
                    {% for prop in produits %}
                        <div class="col-3">
                            <div clas="card">
                                <div clas="card-body">
                                    <h5 clas="card-title">
                                        <a href="{{ path('prop.show', {id: prop.idProduit, slug: prop.slug}) }}"> {{prop.nomProduit}} </a>
                                        {# <a href="#"> {{prop.nomProduit}} </a> #}
                                    {# </h5>
                                    <p class="card-text"> {{prop.description }} </p>
                                    <div class="text-primary">{{prop.prix}} €</div>
                                </div>
                            </div>
                        </div>
                    {% endfor %}
                </div>
            </div> #} 
    </body>
{% endblock %}